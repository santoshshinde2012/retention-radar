{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.local/schemas/hitl_review_log.schema.json",
  "title": "HITL review log row",
  "description": "One human review decision after scoring. Start of predict→act→outcome; outcome write-back deferred.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "user_id",
    "p_cal",
    "band",
    "action_suggested",
    "reviewer",
    "action_taken",
    "notes",
    "timestamp"
  ],
  "properties": {
    "user_id": { "type": "string", "minLength": 1 },
    "p_cal": {
      "type": ["number", "string"],
      "description": "Calibrated P(churn) at score time"
    },
    "band": {
      "type": "string",
      "enum": ["low", "medium", "high"]
    },
    "action_suggested": {
      "type": "string",
      "description": "HITL action from policy (monitor / nurture / outreach / escalate)"
    },
    "reviewer": {
      "type": "string",
      "description": "Human reviewer id or handle (empty until filled)"
    },
    "action_taken": {
      "type": "string",
      "description": "What the reviewer actually did (may differ from suggested)"
    },
    "notes": { "type": "string" },
    "timestamp": {
      "type": "string",
      "description": "ISO-8601 UTC timestamp of the review append"
    }
  }
}
